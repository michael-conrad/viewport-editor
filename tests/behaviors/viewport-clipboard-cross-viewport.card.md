Use viewport-editor MCP tools. Open `dorian-gray.txt` in viewport A using the `viewport` tool. Navigate to a line containing "odour" and copy it using the `clipboard` tool with action=copy. Open the same file in viewport B using the `viewport` tool. Navigate to line 1 in viewport B and paste using the `clipboard` tool with action=paste to insert the copied content before line 1. Show the diff in viewport B using the `diff` tool.