Use the viewport-editor MCP tools to open `dorian-gray.txt`, replace every occurrence of "odour" with "odor", show the diff, save, and close. The file is in the project root directory. Use the viewport-editor `viewport` tool to open and close. Use the viewport-editor `edit` tool for the replacement. Use the viewport-editor `diff` tool to show pending changes. Use the viewport-editor `file` tool to persist to disk.