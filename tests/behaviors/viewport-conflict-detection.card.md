Use viewport-editor MCP tools. Open `dorian-gray.txt` using the `viewport` tool. Replace "odour" with "odor" in the first paragraph using the `edit` tool. Then use bash to append a new line to the file on disk (simulating an external modification). Try to save using the `file` tool — the save should be rejected with a hard conflict error. Make another edit (replace "beautiful" with "lovely") using the `edit` tool — the edit should succeed with a soft warning about the external conflict. Show the diff using the `diff` tool.