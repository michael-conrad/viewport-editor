Use viewport-editor MCP tools. Open `dorian-gray.txt` using the `viewport` tool. Find the line containing "odour" and copy it using the `clipboard` tool. Stash the clipboard content with name "first-copy" using the `clipboard` tool with action=stash. Find the line containing "beautiful" and copy it using the `clipboard` tool. Stash with name "second-copy" using the `clipboard` tool with action=stash. Pop "first-copy" back to clipboard using the `clipboard` tool with action=pop. Show the clipboard content using the `clipboard` tool with action=show — it should contain the content from "first-copy". Swap clipboard with "second-copy" using the `clipboard` tool with action=swap — the clipboard should now hold "second-copy" content.