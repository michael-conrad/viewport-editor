Use viewport-editor MCP tools. Open `dorian-gray.txt` using the `viewport` tool. Search for the pattern "odour" using the `search` tool with action=find. Jump to the first result using the `viewport` tool with action=jump. Edit that line to replace "odour" with "aroma" using the `edit` tool. Perform a regex search for lines containing both "beautiful" and "good" using the `search` tool with action=find and regex=true. Show the combined diff of all changes using the `diff` tool. Save using the `file` tool and close using the `viewport` tool.