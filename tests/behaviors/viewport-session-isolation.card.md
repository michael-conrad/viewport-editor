Open `dorian-gray.txt` twice using the `viewport` tool — first as viewport A, then as viewport B. In viewport A, replace all "odour" with "odor" using the `edit` tool. Do not save. In viewport B, replace all "beautiful" with "stunning" using the `edit` tool. Show the diff in viewport A. Show the diff in viewport B. Report whether the diffs are identical or different, and what this implies about buffer sharing between viewports within one session.