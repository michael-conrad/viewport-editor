Use the viewport-editor MCP tools: open `dorian-gray.txt` with autosave disabled using the `viewport` tool. Replace every occurrence of "odour" with "odor" using the `edit` tool. Show the pending diff using the `diff` tool — the file on disk should NOT yet contain "odor". Toggle autosave on using the `viewport` tool with autosave=true. Close the viewport using the `viewport` tool. After close, the file on disk should contain "odor".