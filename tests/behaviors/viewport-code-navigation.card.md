Use viewport-editor MCP tools. Open `example.py` using the `viewport` tool. Jump to the class `ViewportConfig` using the `viewport` tool with action=jump. Jump to the function `edit_buffer` using the `viewport` tool with action=jump. Search for the symbol `create_viewport` using the `search` tool with action=find. Edit the `edit_buffer` function body to change its return value to `True` explicitly using the `edit` tool. Jump to the constant `DEFAULT_PORT` using the `viewport` tool with action=jump. Show the diff of all changes using the `diff` tool. Save using the `file` tool.